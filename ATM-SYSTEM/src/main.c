#include <stdio.h>
// #include <stdbool.h>
#include "header.h" // Include the header file


int main()
{
    // Users *usersTable = NewUsersTable();
    // Insertion(usersTable, 1, "BenDoe", "Ben123");
    // // Insertion(usersTable, 1, "MikeRoss", "Mike123");
    // // Insertion(usersTable, 1, "TouriaArghabi", "Ben123");
    // AcountCreation(usersTable, "BenDoe", 666, "01/02/2023", "Morroco", 123456789, 356456.568, "Saving");
    // AcountCreation(usersTable, "BenDoe", 123, "01/02/2023", "Morroco", 123456789, 356456.568, "Saving");
    // AcountCreation(usersTable, "BenDoe", 789, "01/02/2023", "Morroco", 123456789, 356456.568, "Saving");
    // AcountCreation(usersTable, "BenDoe", 53, "01/02/2023", "Morroco", 123456789, 356456.568, "Saving");
    // AcountCreation(usersTable, "BenDoe", 357, "01/02/2023", "Morroco", 123456789, 356456.568, "Saving");
    // // // printf("The user id is: %d, his user name is %s and his password is: %s the acount id is %d\n", usersTable->HashedUsers[12]->Id, usersTable->HashedUsers[12]->UserName, usersTable->HashedUsers[12]->Password, usersTable->HashedUsers[12]->Acounts->AcountId);
    // // // printf("The user id is: %d, his user name is %s and his password is: %s\n", usersTable->HashedUsers[8]->Id, usersTable->HashedUsers[8]->UserName, usersTable->HashedUsers[8]->Password);
    // // // printf("The user id is: %d, his user name is %s and his password is: %s\n", usersTable->HashedUsers[12]->Id, usersTable->HashedUsers[12]->UserName, usersTable->HashedUsers[12]->Password);
    // // // printf("The user id is: %d, his user name is %s and his password is: %s\n", usersTable->HashedUsers[4]->Id, usersTable->HashedUsers[4]->UserName, usersTable->HashedUsers[4]->Password);
    // // // Acount *acount = NewAcount(1, "01/02/2023", "Morroco", 123456789, 356456.568, "Saving");
    // // // printf("The acount id is %d, %s, %s, %d, %f, %s\n",acount->AcountId, acount->CreationDate, acount->Country, acount->AcountNumber, acount->Balance, acount->AcountType);
    // // // bool check = UserExists("Ben", "Ben");
    // // // printf("%d", check);
    // // AcountDeletion(usersTable, "BenDoe", 789);
    //  DisplayUserAcounts(usersTable, "BenDoe");
    // InitialMenu();
    // ProfileMenu(usersTable, usersTable->HashedUsers[HashedIndex("BenDoe")]);
    // ReadFile("data/records.txt");
     UserRegistration();
    return 0;
}
